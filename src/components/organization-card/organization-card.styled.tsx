import { Card } from "@mui/material"
import { styled } from "@mui/material/styles"

export const OrganizationCardRoot = styled(Card)(({ theme }) => ({
  borderRadius: theme.spacing(1.5),
  border: "1px solid #d9e2f2",
}))
